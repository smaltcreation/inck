{% trans_default_domain 'ArticleBundle' %}

<div class="container-fluid">
    <div class="row">
        <div class="col-xs-12">
            <div class="table-responsive">
                <table class="table table-striped table-hover">
                    {% for tag in entities %}
                        <tr>
                            <td>
                                <div>
                                    <span class="pull-right">
                                        {{ subscribe_button('tag', tag, 'btn-xs') }}
                                    </span>
                                    <h4>
                                        <a href="{{ path('inck_article_tag_show', { 'id': tag.id, 'slug': tag.slug }) }}" target="_blank">
                                            {{ tag.name|trans }}
                                        </a>
                                    </h4>
                                </div>
                            </td>
                        </tr>
                    {% endfor %}
                </table>
            </div>

            <div class="navigation">
                {{ knp_pagination_render(entities) }}
            </div>
        </div>
    </div>
</div>
