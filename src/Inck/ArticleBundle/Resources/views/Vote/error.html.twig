{% trans_default_domain 'ArticleBundle' %}
<div class="modal fade" id="vote-error">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal">
                    <span aria-hidden="true">&times;</span>
                    <span class="sr-only">{{ 'vote.error.close'|trans }}</span>
                </button>
                <h4 class="modal-title">{{ 'vote.error.error_accured'|trans }}</h4>
            </div>
            <div class="modal-body">
                <p>{{ message }}</p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-default" data-dismiss="modal">
                    <i class="fa fa-times"></i> {{ 'vote.error.close'|trans }}
                </button>
            </div>
        </div>
    </div>
</div>