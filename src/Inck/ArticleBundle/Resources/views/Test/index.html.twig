{% trans_default_domain 'ArticleBundle' %}
<h1>
    {{ user.username }}
    <small>
        {{ articles|length }} {{ 'test.index.published_articles'|trans }}
    </small>
</h1>

{% for article in articles %}
    <p>
        {{ article.title }}
    </p>
{% endfor %}