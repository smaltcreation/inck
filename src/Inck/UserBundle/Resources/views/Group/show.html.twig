{% extends "InckUserBundle::layout.html.twig" %}
{% block title %}{{ group.getName() }}{% endblock %}

{% block header %}
    <div class="jumbotron">
        <div class="container">
            <h1>{{ group.getName() }}</h1>
            <p>
                <a href="{{ path('fos_user_group_edit', {'groupName': group.getName()} ) }}" class="btn btn-default"><i class="fa fa-edit"></i> Editer le groupe</a>
                <a href="{{ path('fos_user_group_edit', {'groupName': group.getName()} ) }}" class="btn btn-primary"><i class="fa fa-users"></i> Ajouter des utilisateurs</a>
            </p>
        </div>
    </div>
{% endblock %}

{% block content %}
    <div class="container">
        {% include "InckUserBundle:Group:show_content.html.twig" %}
    </div>
{% endblock content %}
