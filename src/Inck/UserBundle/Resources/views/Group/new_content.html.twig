{% trans_default_domain 'FOSUserBundle' %}

<form action="{{ path('fos_user_group_new') }}" {{ form_enctype(form) }} method="POST" class="fos_user_group_new group">
    {{ form_errors(form) }}

    <section>
        <div class="well">
            <div class="row">
                <div class="col-xs-12">
                    {% image '@InckUserBundle/Resources/public/images/bag@2x.png' %}
                        <img class="img-responsive img-inline" src="{{ asset_url }}" alt="Sac"/>
                    {% endimage %}
                    <h2>A quoi sert un groupe ?</h2>
                    <p class="description">
                        Un groupe permet d'acheter les droits de diffusion d'articles aux enchères.
                        En effet, ce système permet de rémunérer nos auteurs tout en aidant la presse dans son but de proposer un contenu libre et culturel.
                        Tout le monde peut créer un groupe où un nombre illimité d'utilisateur peut être ajouté.<br>
                        Vous êtes une grande entreprise ? Un acteur majeur de la presse ? Ou bien un petit blogueur recherchant un contenu de qualité ? N'hésitez plus !
                    </p>
                </div>
            </div>
            <div class="row">
                <div class="col-sm-6">
                    {{ form_row(form.name) }}
                </div>
                <div class="col-sm-6">
                    {{ form_row(form.email) }}
                </div>
            </div>
            <div class="row">
                <div class="col-sm-6">
                    {{ form_row(form.website) }}
                </div>
                <div class="col-sm-6">
                    {{ form_row(form.type) }}
                </div>
            </div>
            <div class="row">
                <div class="col-sm-6">
                    {{ form_row(form.private) }}
                </div>
            </div>

            <button class="btn btn-success" type="submit"><i class="fa fa-plus"></i> {{ 'group.new.submit'|trans }}</button>
        </div>
    </section>

    <section>
        <div class="well">
            <div class="row">
                <div class="col-xs-12">
                    {% image '@InckUserBundle/Resources/public/images/Map-drop@2x.png' %}
                    <img class="img-responsive img-inline" src="{{ asset_url }}" alt="Carte"/>
                    {% endimage %}
                    <h2>Coordonnées</h2>
                    <p class="description">
                        En renseignant vos coordonnés, nous pouvons vérifier puis certifier votre compte.
                        Vous vous appelez Le Monde, Google ou bien Twitter ? Laissez-nous dire à nos utilisateurs que vous êtes les vrais.
                    </p>
                </div>
            </div>
            <div class="row">
                <div class="col-sm-6">
                    {{ form_row(form.address) }}
                </div>
                <div class="col-sm-6">
                    {{ form_row(form.city) }}
                </div>
            </div>
            <div class="row">
                <div class="col-sm-6">
                    {{ form_row(form.zipcode) }}
                </div>
                <div class="col-sm-6">
                    {{ form_row(form.country) }}
                </div>
            </div>

            <div class="hidden">
                {{ form_rest(form) }}
            </div>

            <button class="btn btn-success" type="submit"><i class="fa fa-plus"></i> {{ 'group.new.submit'|trans }}</button>
        </div>
    </section>
</form>